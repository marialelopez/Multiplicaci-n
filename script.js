alert("MULTIPLICACIÓN");

// Paso a paso
alert("Vamos a empezar");
var numeroA = prompt("Digite el primer numero");
var numeroB = prompt(" Digite el segundo numero");

// Convertir cadenas a enteros usando parseInt
numeroA = parseInt(numeroA);
numeroB = parseInt(numeroB);

var resultado = numeroA * numeroB;

// Mostrar el resultado usando la función alert

alert("El resultado es: " + resultado);